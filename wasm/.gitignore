# Third-party clones fetched per build environment (large; see NOTES.md to set up)
/cbang/
/emsdk/
/libexpat/

# Emscripten build objects + archives
/wobj/

# Logs and generated reference/demo data
*.log
/real_*.json
/real_*.stl
# (the built wasm core lands in app/src/wasm/ — ignored by wasm/app/.gitignore)
